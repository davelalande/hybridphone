MyApp.sorted = function (params) {
	sortOptions = new DevExpress.data.DataSource([]);

    var viewModel = {
		//  Put the binding properties here
		message: 'Get misc!',

		
    };

    return viewModel;
};